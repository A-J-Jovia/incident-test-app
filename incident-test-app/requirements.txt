incident-test-app/requirements.txt
---
# The below line was removed as it was an invalid requirement
# error: no such option: --- 

# If any specific package or its version was intended to be specified here, replace this with the correct line (e.g., pip install -r requirements.txt)
# pip install --upgrade pip 

# No other changes made; this file is now clean, and a pip install -r requirements.txt should now run without errors.